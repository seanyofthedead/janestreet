{
  "name": "janestreet-trading",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "engine",
    "dashboard",
    "watchdog",
    "backtest"
  ],
  "scripts": {
    "build:ocaml": "bash scripts/build-ocaml.sh",
    "dev:engine": "npm run dev --workspace=engine",
    "dev:dashboard": "npm run dev --workspace=dashboard",
    "dev:watchdog": "npm run dev --workspace=watchdog"
  }
}
